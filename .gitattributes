# LF en todos lados. La suite se edita desde el host (donde vive como
# submódulo) y desde acá: con finales mezclados, cualquier archivo tocado
# aparece como reescrito entero. Y los scripts de infra se pegan tal cual en
# CloudShell, donde bash falla con CRLF ($'\r').
* text=auto eol=lf
